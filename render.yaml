services:
  - type: web
    name: recuperacion-cuenta
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB
        value: recuperacion_cuenta
      - key: ADMIN_KEY
        sync: false
